name: example
description: A new Flutter project.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  pie_chart:
    path: ../packages/pie_chart

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
